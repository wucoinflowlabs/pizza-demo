# Server-only. Never prefix these with NEXT_PUBLIC_.
PAYMENTS_API_BASE_URL=https://api-sandbox.coinflow.cash/api
# Admin-scoped API key for Adora's parent merchant account (merchantId "theza").
PAYMENTS_API_KEY=

# Signs invite links and session cookies. Any long random string.
APP_SECRET=
# Passcode for Adora's internal operator screen (/operator).
OPERATOR_PASSCODE=
