/**
 * Treat an application as approved as soon as it's submitted, and wire up
 * settlement right away. Set to false to show the "under review" screen
 * until the provider actually unblocks the account.
 */
export const ASSUME_APPROVED_ON_SUBMIT = true;
